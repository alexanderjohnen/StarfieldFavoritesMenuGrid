Added: controller and arrow-key navigation of the grid - up and down change the row, left and right step through the slots
Added: WrapNavigation in the INI - whether the ends of a row and of the row stack are walls or doors (off by default)
Fixed: rows no longer leak between saves - each save keeps its own state file, so loading an older save does not bring in the favorites of the run you just left (reported by fabs1)
Fixed: the quantity in brackets now counts down as you use an item - it came from the wheel's item card, which is not refreshed while the grid hides the wheel (reported by ZombieMonkeyNZ - not a StarUI or Free Lanes conflict)
Fixed: a slot whose item you no longer carry stays dimmed - it used to turn bright again after saving, after closing the menu, or after changing rows
Fixed: items the game does not store as plain inventory favorites were never dimmed at all - that exemption is meant for powers
Fixed: an icon is no longer lost for good when the wheel hands over a card without one
Fixed: a reserved slot with no icon now shows the item's name, as grid cells already did
Fixed: favorites first met in a save land on your default row rather than on row 1
Changed: the INI has two sections now, [Grid] and [Controls] - your existing values are picked up from their old places, nothing needs editing
Removed: Enabled, HideWheel and Icons - there was no setting of them that made sense
Known: a save this mod has never seen shows editor IDs instead of icons in row 1 until the favorites menu has been in edit mode once
